import { JSX } from "react";
import { TimerButton } from "../../entities/footer/ui/TimerButton/TimerButton";

export const TimerButtonFeature = (): JSX.Element => {
  return (
    <TimerButton />
  )
}