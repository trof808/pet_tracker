import { JSX } from "react";
import { SettingsButton } from "../../entities/footer/ui/SettingsButton/SettingsButton";

export const SettingsButtonFeature = (): JSX.Element => {
  return (
    <SettingsButton />
  )
};