import { JSX } from "react";
import { StatsButton } from "../../entities/footer/ui/StatsButton/StatsButton";

export const StatsButtonFeature = (): JSX.Element => {
  return (
    <StatsButton />
  )
}